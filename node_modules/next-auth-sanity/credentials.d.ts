import type { CredentialsProvider } from 'next-auth/providers';
import type { SanityClient } from '@sanity/client';
declare type CredentialsConfig = ReturnType<CredentialsProvider>;
export declare const signUpHandler: (client: SanityClient, userSchema?: string) => (req: any, res: any) => Promise<void>;
export declare const SanityCredentials: (client: SanityClient, userSchema?: string) => CredentialsConfig;
export {};
